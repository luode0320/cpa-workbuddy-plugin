---
schema_version: 1
doc_id: TEST-TRAE-DYNAMIC-MODELS-20260830
doc_type: test
source_ids: [BUG-TRAE-CONTENT-AND-MODEL]
status: accepted
version: v1.0
current_slice: Trae 账号级动态模型发现
updated_at: 2026-08-30 02:55:11
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# Trae 动态模型发现代码级验证

结论：账号级动态模型发现的本地代码验证通过。影响：模型选择器可使用上游返回的真实模型 ID，减少手工短 ID 导致的参数错误。范围：动态接口请求、模型解析、去重、展示名回退和失败降级。非范围：本轮没有发布到生产，也没有把本地测试结果当作生产对话验收。变化：`model.for_auth` 成功时返回账号实时模型，失败时保留配置或内置列表。完成标准：构建、静态检查和测试全部通过，且失败分支不会清空模型列表。术语说明：`config_name` 是 Trae 对话接口真正接受的模型标识。验证状态：本地真实执行通过；生产部署验证未执行，原因：动态模型代码尚未发布，证据：版本仍为 `0.1.13`，本轮未执行发布链路。

## 验证结论

- 代码级验证：通过。
- 生产对话验证：N/A。原因：本轮动态模型功能未发布。证据：版本仍为 `0.1.13`，未执行版本升级、提交、推送、CI 或安装。
- 浏览器联调：N/A。原因：本轮修改为 Go 插件模型能力，没有页面交互变更。证据：改动文件均位于 `traework/`。
- 图片资产决策：N/A。原因：验证对象是接口请求和 Go 逻辑，不需要 UI 或视觉证据。证据：全部断言来自自动化测试输出。

## 测试策略

| 优先级 | 验证对象 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| P0 | 动态模型请求契约 | 本地 `httptest` 上游 + cgo shim | 通过 |
| P0 | 精确 `config_name` 解析 | 自动化断言 | 通过 |
| P0 | 上游失败回退 | 表驱动异常分支测试 | 通过 |
| P1 | Seed 短 ID 兼容 | 模型解析单元测试 | 通过 |
| P1 | 生产账号真实对话 | 未执行。原因：动态模型代码尚未发布。证据：版本仍为 `0.1.13`，本轮未执行发布链路。 | 未执行 |

## 被测文件

- `traework/models.go`
- `traework/upstream.go`
- `traework/models_dynamic_test.go`
- `traework/upstream_model_test.go`

## 测试资产边界

当前 `traework` 是 `package main` 的 c-shared 插件，现有白盒测试均与模块同目录，并由 `scripts/cgo-shim-build.py` 复制到隔离目录执行。根 `test/` 外部包无法导入 main 包或访问 `handleModelForAuth`、`resolveModelOptions` 等未导出符号。本轮不以测试目录治理为由扩大为插件测试架构重构；生产代码没有新增测试专用入口、字段或分支。测试污染扫描结果为 `POLLUTION: PASS`。

## 执行附录

执行命令：

```bash
python scripts/cgo-shim-build.py traework
```

结果摘要：

```text
[cgo-shim] OK: go build ./...
[cgo-shim] OK: go vet ./...
ok github.com/luode0320/cpa-workbuddy-plugin/traework 1.075s
[cgo-shim] OK: go test ./...
[cgo-shim] all green (traework)
```

覆盖范围：

1. 请求方法、路径、认证头及 `solo_work_lite` 请求参数。
2. 重复模型去重、空 ID 忽略、展示名缺失时回退模型 ID。
3. 请求 JSON 无效、缺失账号存储、非 2xx、无效响应 JSON 和空模型列表均回退配置模型。
4. `seed-2.1-pro` 等短 ID 映射到已确认的精确 `config_name`；`qwen3.8-max` 原样透传；`auto` 继续使用 `inline_chat`。

## 追踪附录

| 测试 ID | 对应实现 | 证据 |
| --- | --- | --- |
| TEST-TRAE-DYNAMIC-001 | `handleModelForAuth`、`fetchTraeModels` | `TestHandleModelForAuthDynamicSuccess` |
| TEST-TRAE-DYNAMIC-002 | 动态发现失败回退 | `TestHandleModelForAuthFallbacks` |
| TEST-TRAE-DYNAMIC-003 | `resolveModelOptions` | `TestResolveModelOptionsMapsSeedAliases` 等 3 个测试 |
| EVIDENCE-TRAE-DYNAMIC-001 | 完整模块验证 | cgo shim build/vet/test 全绿 |
