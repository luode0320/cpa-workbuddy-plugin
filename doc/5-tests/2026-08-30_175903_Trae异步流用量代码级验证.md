---
schema_version: 1
doc_id: TEST-TRAE-ASYNC-STREAM-USAGE-20260830
doc_type: test
source_ids: [BUG-TRAE-ASYNC-STREAM-USAGE-20260830]
status: in_progress
version: v1.2
current_slice: Trae 异步流式请求用量发布后的生产验证
updated_at: 2026-08-30 19:35:00
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# Trae 异步流用量代码级验证

结论：异步流结束后发布用量的本地代码验证通过，修复已发布为 `traework-provider 0.1.19`，生产恢复情况仍未验证。影响：修复后的 Trae 插件具备把 WorkBuddy 流式对话写入共享 feed 的代码能力。范围：异步流成功结束、用量字段、单次写入、构建、静态检查和测试污染。非范围：真实 Qwen 请求和面板展示不属于代码级测试。变化：新增 `TestPumpTraeStreamPublishesUsage`，并验证流泵结束后写入一条成功记录。完成标准：污染扫描、build、vet、test、格式和差异检查全部通过，并在生产安装后完成真实异步流式验证。术语说明：异步流是 handler 先返回响应头，再由后台任务持续推送分片的执行方式。验证状态：本地代码、CI、release assets、registry 和 Raw CDN 校验通过；生产 SSH 密钥连接已恢复，生产安装尚未执行。

## 验证结论

- 代码级验证：通过。
- 测试污染：通过，`POLLUTION: PASS`。
- 发布与资产校验：通过。CI 运行 `33307356764` 成功；7 个 release assets 的 checksum 与 GitHub Raw CDN 文件均已复核。
- 生产 SSH 连通性：通过。证据：使用 `cpa-server` 别名与专用密钥连接 `45.207.222.65:18998` 成功，远端身份为 `root`，主机名为 `ser736666210288`。
- 生产端到端验证：未执行。原因：本轮仅完成 SSH 配置与无害连通性验证，尚未调用 plugin-store 安装接口或发起 Qwen 请求。
- 浏览器联调：N/A。原因：改动是 Go 插件后台记账逻辑，没有前端页面代码变化。证据：目标文件均位于 `traework/`。
- 图片资产决策：N/A。原因：验证对象是调用链和 NDJSON 记录。证据：自动化断言可以核对全部字段。

## 测试策略

| 优先级 | 验证对象 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| P0 | 异步流成功结束后发布用量 | 自动化回归测试 | 通过 |
| P0 | feed 记录模型、provider、账号与结果字段 | JSON 断言 | 通过 |
| P0 | 生产代码测试污染 | 差异扫描 | 通过 |
| P0 | 模块可构建和测试 | cgo shim build、vet、test | 通过 |
| P1 | 生产 WorkBuddy 流式请求 | 发布后真实请求 | N/A。原因：修复尚未发布。证据：本轮未修改版本、registry 或 release assets。 |

覆盖质量维度：功能、异常健壮性和兼容性。性能、界面、安全与易用性 N/A，原因是本补丁没有新增接口、页面、权限或高频同步阻塞，证据是 feed 写入继续位于 `publishUsage` 的后台 goroutine。

## 被测文件

- `traework/executor.go`
- `traework/stream.go`
- `traework/usage.go`
- `traework/usage_feed.go`
- `traework/usage_feed_test.go`

## 测试资产边界

当前插件是 `package main` 的 c-shared 模块，现有白盒测试位于模块目录，并由 `scripts/cgo-shim-build.py` 复制到隔离目录执行。本轮沿用存量测试模式，不新增生产测试专用函数、字段、分支或构造器。测试污染扫描命中 0 条，结果为 `POLLUTION: PASS`。

## 完成标准

1. `TestPumpTraeStreamPublishesUsage` 生成一条 feed 记录。
2. 记录字段满足 `alias=qwen-max-latest`、`model=qwen3.8-max`、`provider=traework-provider`、`auth_index=uid-1`。
3. 记录为成功结果，`failed=false`、`status_code=0`。
4. build、vet、test 全部通过。
5. 生产代码测试污染扫描通过。

## 执行附录

执行命令：

```bash
python scripts/cgo-shim-build.py traework
python test-strategy-rules/scripts/scan_test_pollution.py --root . --diff-only
git diff --check -- traework/executor.go traework/stream.go traework/usage_feed_test.go
gofmt -l traework/executor.go traework/stream.go traework/usage_feed_test.go
```

结果摘要：

```text
扫描生产文件 3 个，命中 0 条。
POLLUTION: PASS
[cgo-shim] OK: go build ./...
[cgo-shim] OK: go vet ./...
ok github.com/luode0320/cpa-workbuddy-plugin/traework 1.388s
[cgo-shim] OK: go test ./...
[cgo-shim] all green (traework)
```

`git diff --check` 退出码为 0，`gofmt -l` 没有输出目标文件。

人工生产验证步骤：

1. 发布并安装包含本补丁的新 `traework-provider` 版本。
2. 记录共享 feed 当前行数。
3. 从 WorkBuddy 发起一次异步流式 `qwen-max-latest` 请求并确认响应完成。
4. 核对 feed 只增加一条记录，字段包含 `provider=traework-provider`、`alias=qwen-max-latest`、`model=qwen3.8-max`。
5. 通过 `workbuddy-token-usage` 的 requests 数据源按时间查询该记录。

### 已解除的 SSH 阻断

`BLK-TRAE-SSH-001` 已于 2026-08-30 19:35 解除：`cpa-server` 的密钥认证无害连接已成功。真实验证尚未开始，因为本轮仅完成 SSH 配置与连接验证。

## 追踪附录

| 测试 ID | 对应实现 | 证据 |
| --- | --- | --- |
| TEST-TRAE-ASYNC-USAGE-001 | `pumpTraeStream` 成功终点 | `TestPumpTraeStreamPublishesUsage` |
| TEST-TRAE-ASYNC-USAGE-002 | feed 字段保持别名与上游模型 | 测试 JSON 字段断言 |
| TEST-TRAE-ASYNC-USAGE-003 | 无生产测试污染 | `POLLUTION: PASS` |
| EVIDENCE-TRAE-ASYNC-USAGE-001 | 模块代码级验证 | cgo shim build、vet、test 全绿 |
| EVIDENCE-TRAE-ASYNC-USAGE-002 | 格式与空白检查 | `gofmt -l` 无输出，`git diff --check` 退出码 0 |
