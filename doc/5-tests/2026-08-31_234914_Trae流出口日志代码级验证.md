---
schema_version: 1
doc_id: TEST-TRAE-STREAM-LOG-20260831
doc_type: test
source_ids: [TASK-TRAE-STREAM-LOG-20260831]
status: in_progress
version: v1.0
current_slice: Trae 流出口日志插桩代码级验证
updated_at: 2026-08-31 23:49:14
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# Trae 流出口日志插桩代码级验证

结论：为 Trae 流式三出口与流式执行入口补日志插桩的代码级验证通过。影响：发布后生产 `docker logs cli-proxy-api` 可按 `request_id` / `stream_id` 拉全链路日志，判断「上游是否真正 done / output_eof 截断 / 空响应」，用于现场抓取「生成中途停止」的流形态。范围：cgo shim 的 build、vet、test，格式与空白检查，测试真实参与编译。非范围：真实 Qwen 请求、生产发布、生产日志抓取不属于代码级测试。变化：`traework/stream.go` 三出口新增 `[traework] stream collect/aggregate/pump ...` 日志与 `terminationLabel` helper；`traework/executor.go` 流式执行入口补账号维度日志。完成标准：build、vet、test 全绿，`git diff --check` 为 0，哨兵测试证明测试真实参与编译。验证状态：代码级验证通过；生产日志可见性留待发布后现场验证。

## 验证结论

- 代码级验证：通过。cgo shim build、vet、test 全绿（1.143 秒）。
- 测试真实参与编译：通过。临时必失败哨兵测试被 shim 执行并 FAIL，输出同时可见新加的 pump 日志行，证明新增代码与测试真实进入编译；删除哨兵重跑全绿。
- 格式与空白：通过。`git diff --check -- traework` 退出码为 0。
- 日志通道：通过。插件 `log.Printf` 输出会出现在生产 `docker logs cli-proxy-api`（`[keepalive] daily run` 先例验证于 2026-08-31 22:00）。
- 生产端到端验证：未执行。原因：本轮仅完成代码级日志插桩，尚未发布与部署；发布后由用户触发一次中断会话，再从生产日志抓取流形态。
- 浏览器联调：N/A。原因：改动是 Go 插件日志，没有前端页面代码变化。
- 图片资产决策：N/A。原因：验证对象是日志输出与编译结果。证据：无视觉改动。

## 测试策略

| 优先级 | 验证对象 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| P0 | 模块可构建和测试 | cgo shim build、vet、test | 通过 |
| P0 | 测试真实参与编译 | 临时必失败哨兵测试 | 通过（FAIL 证明进编译，删除后重跑全绿） |
| P0 | 格式与空白 | `git diff --check` | 通过 |
| P1 | 日志三出口覆盖 | 代码走查 `collectTraeStream` / `aggregateTraeCompletion` / `pumpTraeStream` 的 error/invalid/done/truncated 出口 | 通过 |
| P1 | 生产日志可见性 | 发布后 `docker logs cli-proxy-api` 抓取 | N/A。原因：尚未发布。证据：本轮未修改版本、registry 或 release assets。 |

覆盖质量维度：功能（日志出口）、可观测性。性能、界面、安全与易用性 N/A，原因是补丁只读新增日志，不改变业务分支或发布路径。

## 被测文件

- `traework/stream.go`（collect / aggregate / pump 三出口日志 + `terminationLabel`）
- `traework/executor.go`（流式执行入口账号维度日志）

## 测试资产边界

本轮沿用存量 cgo shim 隔离测试模式，不新增生产测试专用函数、字段、分支或构造器。哨兵测试为临时验证资产，验证后已删除，无残留。

## 完成标准

1. cgo shim build、vet、test 全部通过。
2. 临时必失败哨兵测试 FAIL，证明测试与新增代码真实参与编译；删除后重跑全绿。
3. `git diff --check -- traework` 退出码为 0。
4. 三出口日志均带可检索键（`request_id` / `stream_id` / `model` / `status` / `termination`）与 `truncateRedacted` 脱敏。

## 执行证据

```text
[cgo-shim] OK: go build ./...
[cgo-shim] OK: go vet ./...
ok github.com/luode0320/cpa-workbuddy-plugin/traework 1.143s
[cgo-shim] OK: go test ./...
[cgo-shim] all green (traework)

# 哨兵测试阶段（证明测试真实参与编译）
--- FAIL: TestSentinelMustFailToProveCompilation
    sentinel_tmp_test.go:6: sentinel: this must run to prove tests compile
FAIL  github.com/luode0320/cpa-workbuddy-plugin/traework
[cgo-shim] FAILED: go test ./... (exit 1)

# 哨兵阶段测试输出已出现新加 pump 日志行
2026/08/31 23:48:20 [traework] stream pump finish emit error: ... stream_id=stream-test ...
2026/08/31 23:48:20 [traework] stream pump error: request_id=749a72c3... stream_id=stream-trunc ... elapsed_ms=0

# 删除哨兵后重跑
[cgo-shim] all green (traework)
```

## 后续验证交接

1. 完成 0.1.23 发布与生产部署（plugin-store install + hot reload）。
2. 用户用 `sess_3179110d` 再触发一次中断会话。
3. 从生产抓取 `docker logs cli-proxy-api --since <发布时刻> | grep "\[traework\]"`，按 `request_id` / `stream_id` 定位该次请求。
4. 断言：日志给出 `termination=done`（业务完整）、`output_eof`（上游断流截断）或 `invalid`（空响应）之一，并核对 `finish` / `chunks` / `elapsed_ms` 与用户所见中断形态一致。
