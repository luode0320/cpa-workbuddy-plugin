---
schema_version: 1
doc_id: TEST-TRAE-POOL-EXHAUSTED-3FIX-20260902
doc_type: test
source_ids: [BUG-TRAE-POOL-EXHAUSTED-20260902]
status: accepted
version: v1.0
current_slice: traework 0.1.30 三 FIX local 全回归
updated_at: 2026-09-02
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# Trae 池耗尽三缺陷修复回归（0.1.30）

结论：异步 401 核算驱逐、伪完成单号池同号退避、reasoning 纳入伪完成健康判据三处代码级修复的本地回归全部通过。影响：死号 225774 反复 401 不再因不核算而每请求被 session 亲和重绑拖垮池；单健康号被窗口性节流判伪后可在同请求内同号退避恢复（生产实证 2351：同号约 30s 自愈）；思考型模型长 reasoning + 短正文不再被误判伪完成。范围：`isPseudoCompletion` 判据、async 401 open error 路径、sync/async 伪完成协调分支、单号池同号重试新行为。非范围：本记录不证明 0.1.30 已发布，不证明生产 `/v1/responses` 已通过，不涉及 pump gate reasoning 流式放行（属独立优化已回退）。变化：`stream.go` 判据双计 + reasoning-only 豁免、`executor.go` 两协调器同号重试标签与 401 核算、新增 `test/traework/executor_same_auth_retry_test.go`。完成标准：负向哨兵 + 定向 Fatal 探针证明新测试真实编译执行，移除后 build、vet、test 全绿，gofmt 干净、UTF-8 通过、diff 限于计划内文件。验证状态：本地代码级验证通过，发布与生产行为验收未执行。图片资产决策：N/A，原因：验证对象是 Go 状态机与账号协调分支；证据：没有页面或视觉输出。

## 文档信息

- 来源：2026-09-02 生产取证（0.1.29 部署后 21:26-21:30，stream 2331-2350 失败链 + 2351 同号恢复反证）。
- 关联记忆：`[[traework-20260902-pool-exhausted-rootcause]]`（根因与取证）、`[[traework-prod-account-225774-dead-203343-refresh-mismatch]]`（账号归因纠偏）。
- 环境：Windows 工作树，cgo-shim 使用 `CGO_ENABLED=0` 的临时插件副本；没有连接数据库、缓存、消息队列或 HTTP 上游。

## 验证结论

本地功能与回归结论为通过。三个修复各自有确定性自动化覆盖，且既有 5 个伪完成回归用例（A→B 切换、池耗尽显式失败、同请求不重复选号、reasoning-only 豁免）全部保持通过——证明同号重试只在"无其它候选"时触发，不破坏多候选 A→B 切换的既有契约。

## 回归范围

| 优先级 | 验证对象 | 用例 | 结果 |
| --- | --- | --- | --- |
| P0 | FIX-A 单号池伪完成→同号退避→成功（async） | `TestRunTraeAsyncStreamSameAuthRetryWhenNoCandidateThenSucceeds` | PASS |
| P0 | FIX-A 单号池伪完成→同号退避→成功（sync） | `TestRunTraeSyncStreamSameAuthRetryWhenNoCandidateThenSucceeds` | PASS |
| P0 | FIX-A 成功后失败核算清零 | 上述两用例 `failoverStateSnapshot==0` 断言 | PASS |
| P0 | FIX-C reasoning-only 永不判伪 | `TestIsPseudoCompletion`（content 0 + reasoning>0） | PASS |
| P0 | FIX-C 既有 reasoning-only terminal 语义 | `TestPumpTraeStreamAttemptKeepsExistingTerminalSemantics` | PASS |
| P1 | 既有多候选 A→B 切换不回归 | `TestRunTraeAsyncStreamRetriesPseudoCompletionWithoutLeakingFirstAttempt` / sync 等价 | PASS |
| P1 | 既有全伪完成池耗尽显式失败不回归 | async + sync pool exhaustion 用例 | PASS |
| P1 | 既有同请求不重复选号（A→B→A 拒绝）不回归 | async + sync `DoesNotRetryAnAccountTwice` | PASS |
| P1 | 负向哨兵证明新测试真实编译 | 临时必失败 `TestSentinelMustFail` FAIL 后删除 | PASS |
| P1 | 定向探针证明两新用例被测试框架选中 | 注入无条件 `t.Fatal` 探针双 FAIL 后恢复 | PASS |
| P1 | FIX-B async 401 核算不破坏既有 | async open error 路径全量回归 | PASS |
| P1 | 生产真实入口 | `/v1/responses` | 未执行，不计入本地通过结论 |

## 被测文件

- `traework/stream.go`（isPseudoCompletion 双计 + reasoning-only 豁免 + pseudoRetryBudget 常量）
- `traework/executor.go`（sync/async 伪完成同号退避 + async 401 核算驱逐）
- `test/traework/executor_same_auth_retry_test.go`（新增两用例）

## 补充说明

- FIX-A 的伪完成分支先核算失败 + 驱逐绑定，再 `PickNextAuth`；仅当 `!hasNext || nextSA == nil` 才同号退避重试一次（`pseudoRetryBudget=1`，不消耗跨账号 Budget）。有候选仍走 A→B，保留既有回归语义。
- FIX-C 判据：content 或 reasoning 任一达 600 即健康；content 0 + reasoning>0（reasoning-only）永不判伪；双零返回 false；仅双短且长输入才判伪。
- pump gate（600 字节放行门槛）仍只数 content 未改——reasoning 参与流式 gate 放行属独立优化，超出 0.1.30 缺陷修复范围已回退，避免越界。
