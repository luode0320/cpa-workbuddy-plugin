---
schema_version: 1
template_version: v1
doc_id: STYLE-TRAE-PSEUDO-SAME-REQUEST-20260901
doc_type: style_regression
source_ids: [BUG-TRAE-HTTP200-EMPTY-SUCCESS-20260830, IMPL-TRAE-PSEUDO-SAME-REQUEST-20260901, TEST-TRAE-PSEUDO-SAME-REQUEST-20260901]
status: accepted
version: v1.0
current_slice: TASK-004 完整 local 回归后的风格检查
updated_at: 2026-09-01 22:57:56
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# 6-review：Trae 伪完成同请求换号恢复

结论：本轮代码、测试和脚本通过风格回归。影响：同请求换号实现保持 TraeWork 现有命名、日志、中文元信息、错误出口和依赖方向，新增测试已归位到根 `test/traework/`。范围：`traework/executor.go`、`traework/stream.go`、`scripts/cgo-shim-build.py` 和三份镜像测试。非范围：本记录不判断业务需求是否全部满足，不批准发布，也不把本地测试写成生产验收。变化：单次 attempt 与逻辑请求协调职责分开，账号日志使用短哈希，shim 复制镜像测试并清理完整临时根。完成标准：真实测试先通过，格式、UTF-8、空白、目录、命名、注释、日志、错误处理、复用和临时产物检查均无问题。术语说明：`6-review` 是只检查代码写法和资产位置的风格回归，不判断生产行为。验证状态：真实 cgo-shim 测试已通过，风格检查通过；0.1.27 尚未发布，生产入口尚未验收。图片资产决策：N/A，原因：本记录只核对源码和测试资产；证据：没有 UI 或视觉改动。

## 文档信息

- 来源 Bug：[`Trae HTTP 200 异常响应被静默当作空成功`](../4-bugs/2026-08-30_202238_Trae_HTTP200异常响应被静默当作空成功.md)。
- 实施总览：[`Trae 伪完成同请求换号恢复实施总览`](../3-实施/2026-09-01_225756_Bug-Trae伪完成同请求换号恢复_实施总览.md)。
- 真实测试：[`Trae 伪完成同请求换号恢复回归`](../5-tests/2026-09-01_225756_Trae伪完成同请求换号恢复回归.md)。
- 规则来源：`PROJECT_STYLE.md`、`code-style-consistency-rules` 和本轮代码风格契约。

## 检查范围

- `traework/stream.go`：单次 SSE attempt 结果、健康门槛、缓冲释放和伪完成返回。
- `traework/executor.go`：同步和异步同请求协调器、账号去重、资源关闭、唯一终结和脱敏日志。
- `scripts/cgo-shim-build.py`：根镜像测试注入、文件名冲突失败和临时根清理。
- `test/traework/stream_pseudo_test.go`：599/600、短输入、reasoning-only 和 EOF 边界。
- `test/traework/executor_pseudo_retry_test.go`：同步 A 至 B、池耗尽和账号回跳。
- `test/traework/async_stream_failover_test.go`：异步 A 至 B、callback、唯一 close、emit 失败和 panic 清理。
- 发布放行：N/A。原因：不属于 `6-review` 职责。证据：实施总览中 TASK-005 与 TASK-006 均未执行。

## 真实测试前置证据

- TEST-004：`python scripts/cgo-shim-build.py traework` 的 build、vet、test 全部通过，最新 Go 测试耗时 1.433 秒。
- EVIDENCE-006：两轮唯一失败哨兵先使同一测试入口失败，删除后全绿，证明根镜像测试真实进入编译。
- 静态门禁：`UTF8_BOM: PASS`、`GOFMT: PASS`、`STATIC_GATES: PASS`。
- 污染门禁：修正 shim 临时根清理后复跑同一入口，`POLLUTION: PASS`。

## 6-review 结论

STYLE: PASS

## 检查清单

| 类别 | 结果 | 证据 |
| --- | --- | --- |
| 格式与编码 | PASS | 目标 Go 文件 `gofmt -l` 无输出，UTF-8 无 BOM，差异空白检查通过 |
| 命名与职责 | PASS | `pumpTraeStreamAttempt` 只负责单次上游尝试，`runTraeSyncStream` 与 `runTraeAsyncStream` 负责逻辑请求 |
| 注释 | PASS | 修改函数延续中文参数、返回、修改时间和就近步骤编号 |
| 日志 | PASS | 继续使用 `[traework]` 前缀；账号只记录 `auth_hash`，未回显凭据 |
| 错误处理 | PASS | pool exhaustion、emit 失败、读取错误和 panic 均有明确出口，未吞 Go error |
| 资源生命周期 | PASS | 每个上游 attempt 用局部 defer 关闭，客户端 close 由 `sync.Once` 保证唯一 |
| 复用与依赖 | PASS | 只用 Go/Python 标准库和项目现有 failover、usage、session 函数，没有新增依赖或无收益抽象 |
| 测试资产归位 | PASS | 本轮三份 Go 测试位于根 `test/traework/`，shim 临时复制到同包，不新增生产测试 API |
| 临时产物 | PASS | 无 `cpa-shim-*`、`__pycache__`、`.pyc` 或失败哨兵残留 |
| 改动范围 | PASS | 未修改模型映射、阈值数值、账号冷却算法、宿主 ABI 或其他插件 |
| 业务正确性 | N/A | 原因：由真实测试负责；证据：关联 TEST-001 至 TEST-004 |
| 需求覆盖与发布 | N/A | 原因：不属于风格回归职责；证据：实施总览 AC-001 至 AC-006 |

## 问题与修复

风格检查期间发现 cgo-shim 只删除临时插件子目录，会留下空的 `cpa-shim-*` 父目录。脚本按现有 `pathlib` 与 `shutil.rmtree` 写法新增 `temp_root`，成功和默认失败路径统一删除整个临时根，`--keep` 仍保留原工作目录。复跑真实测试后没有残留。

未发现需要继续修改的格式、命名、注释、日志、可读性、目录或依赖问题。历史源码目录中的其他测试不属于本轮写集，本轮没有借归位扩大迁移范围。

## 执行附录

风格门禁摘要：

```text
UTF8_BOM: PASS
GOFMT: PASS
STATIC_GATES: PASS
POLLUTION: PASS
风格回归已通过。
```

## 追踪附录

| 追踪 ID | 对应内容 | 证据 |
| --- | --- | --- |
| TASK-001 | 健康门槛写法与注释 | `traework/stream.go`、TEST-001 |
| TASK-002 | 同步协调器结构 | `traework/executor.go`、TEST-002 |
| TASK-003 | 异步协调器和生命周期 | `traework/executor.go`、TEST-003 |
| TASK-004 | 测试归位与脚本清理 | `scripts/cgo-shim-build.py`、TEST-004 |
| TEST-001 | 单次 attempt 边界 | `test/traework/stream_pseudo_test.go` |
| TEST-002 | 同步恢复 | `test/traework/executor_pseudo_retry_test.go` |
| TEST-003 | 异步恢复 | `test/traework/async_stream_failover_test.go` |
| TEST-004 | 模块完整回归 | cgo-shim 输出与静态门禁 |
| EVIDENCE-001 | 账号日志脱敏 | `authLogHash` 与 `auth_hash` 日志字段 |
| EVIDENCE-002 | 测试资产归位 | 根 `test/traework/` 文件清单 |
