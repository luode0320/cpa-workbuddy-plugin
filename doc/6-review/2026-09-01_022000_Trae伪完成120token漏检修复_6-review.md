# 6-review：Trae 伪完成检测升级「输出字符 + 输入长度」双重判据

- 日期：2026-09-01 02:20
- 范围：traework-provider 0.1.26 伪完成检测修复（120-token 伪完成漏检）
- 审查维度：代码风格、格式、位置、注释、日志、可读性、目录归位（不判断业务正确性）

## 改动文件

| 文件 | 改动 | 归位 |
|---|---|---|
| traework/stream.go | `isPseudoCompletion` 升级双重判据 + `traeStreamPumpContext.InputChars` | ✅ 核心逻辑在 stream.go |
| traework/executor.go | `estimateInputChars` + 两条路径传入 | ✅ executor 持有消息，计算输入长度 |
| traework/stream_pseudo_test.go | 测试适配 + 新增 `TestEstimateInputChars` | ✅ 测试与逻辑同目录 |
| traework/VERSION / main.go | 0.1.25 → 0.1.26 | ✅ 版本 bump 三件套之一 |
| traework/CHANGELOG.md | 新增 0.1.26 条目 | ✅ 与既有格式一致 |

## 风格检查

- **注释**：中文注释解释常量阈值语义与修复动机；`isPseudoCompletion` 保留英文函数注释（与原文件一致）。✅
- **命名**：`pseudoCompletionMaxChars` / `pseudoCompletionMinInputChars` / `estimateInputChars` 与既有 `estimateUsageFromChunks` 命名风格一致。✅
- **日志**：伪完成日志文案 `"pseudo completion: upstream returned done with near-empty output"` 复用既有常量，未新造。✅
- **格式**：gofmt 全过（CRLF→LF 临时转换校验）；`git diff --check` PASS。✅
- **目录归位**：所有改动均在 traework/ 内，无跨目录漂移。✅

## 结论

STYLE: PASS
