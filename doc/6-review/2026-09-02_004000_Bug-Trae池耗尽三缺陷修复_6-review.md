---
schema_version: 1
template_version: v1
doc_id: STYLE-TRAE-POOL-EXHAUSTED-3FIX-20260902
doc_type: style_regression
source_ids: [BUG-TRAE-POOL-EXHAUSTED-20260902, TEST-TRAE-POOL-EXHAUSTED-3FIX-20260902]
status: accepted
version: v1.0
current_slice: traework 0.1.30 三缺陷修复后的风格检查
updated_at: 2026-09-02 00:40:00
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# 6-review：Trae 池耗尽三缺陷修复（0.1.30）

结论：本轮 stream.go / executor.go 改动通过风格回归。影响：伪完成判据纳入 reasoning 并豁免 reasoning-only，伪完成在无其它候选时同号退避重试一次，async 401 open error 补核算与绑定驱逐——均保持插件现有命名、日志、中文元信息、错误出口与镜像测试归位。范围：`traework/stream.go`、`traework/executor.go` 与 `test/traework/executor_same_auth_retry_test.go`。非范围：本记录不判断业务正确性、不批准发布、不把本地测试写成生产验收。变化：`isPseudoCompletion` 从单计 content 改为 content+reasoning 双计；`pseudoRetryBudget` 常量与 `retrySameAuth` 标签；async open error 401 分支核算驱逐。完成标准：真实 cgo-shim 测试先通过（负向哨兵 + 定向探针），格式、UTF-8、空白、目录、命名、注释、日志、错误处理、复用与临时产物检查均无问题。术语说明：`6-review` 只检查代码写法和资产位置，不判断生产行为。验证状态：真实 cgo-shim 测试已通过，风格检查通过；0.1.30 待发布，生产入口待验收。图片资产决策：N/A，原因：只核对源码与测试资产；证据：无 UI 或视觉改动。

## 文档信息

- 来源取证：2026-09-02 生产失败链（stream 2331-2350）+ 2351 同号恢复反证；关联记忆 [[traework-20260902-pool-exhausted-rootcause]]。
- 回归记录：`doc/5-tests/2026-09-02_003000_Trae池耗尽三缺陷修复回归.md`。
- 真实测试：`test/traework/executor_same_auth_retry_test.go`（async+sync 单号池同号重试成功两用例）。
- 规则来源：`PROJECT_STYLE.md`、`code-style-consistency-rules` 和本轮代码风格契约。

## 检查范围

- `traework/stream.go`：`isPseudoCompletion` 双计判据（content/reasoning 各达 600 健康、reasoning-only 豁免、双零/双短门槛）、`pseudoRetryBudget` 常量与中文注释。
- `traework/executor.go`：async open error 401 分支 `reconcileAfterExecutorError`+`evictSessionBindingsForAuth`；sync/async 伪完成分支 `pseudoTries`/`retrySameAuth` 标签与"无候选才同号退避"结构。
- `test/traework/executor_same_auth_retry_test.go`：两新增用例的断言与 helper 复用。
- 发布放行：N/A。原因：不属于 `6-review` 职责。证据：0.1.30 发布与生产验收尚未执行。

## 真实测试前置证据

- TEST-005：`python scripts/cgo-shim-build.py traework` 的 build、vet、test 全部通过，最新 Go 测试耗时 1.385-1.426 秒。
- EVIDENCE-007：唯一失败哨兵先使测试入口失败（`TestSentinelMustFail` FAIL），删除后全绿，证明新测试真实进入编译。
- EVIDENCE-008：定向无条件 `t.Fatal` 探针注入 async/sync 两新用例，均 FAIL（`DIRECTED-FAIL-PROBE-ASYNC`/`-SYNC`），恢复后全绿——证明两用例被测试框架真实选中执行。
- 静态门禁：`UTF-8: PASS`、`GOFMT: PASS`（CRLF→LF 临时转换后 gofmt -l 为空）、`git diff --check: PASS`。

## 6-review 结论

STYLE: PASS

## 检查清单

| 类别 | 结果 | 证据 |
| --- | --- | --- |
| 格式与空白 | PASS | gofmt -l 三文件全空（LF 临时转换校验）；无尾随空白 |
| 编码 | PASS | 三文件 UTF-8 无 BOM；`git diff --check` 无空白错误 |
| 目录归位 | PASS | 新测试在根 `test/traework/`，不新增源码目录测试 |
| 命名 | PASS | `pseudoRetryBudget`/`pseudoTries`/`retrySameAuth` 与既有 pseudo 命名一致 |
| 注释 | PASS | 中文元信息说明约束（判据语义、2351 依据、同号重试理由），不解释"改了什么" |
| 日志 | PASS | 新增日志含 request_id/stream_id/model/auth_hash/attempt/chunks，与既有一致 |
| 错误处理 | PASS | 复用既有 `reconcileAfterExecutorError`/`noteForcedAccountFailure`/`evictSessionBindingsForAuth`，无重复实现 |
| 复用 | PASS | 测试复用 `streamTestResponse`/`countFinishReason`/`resetFailover`/`disableUsageOutputs`/`failoverStateSnapshot` |
| 临时产物 | PASS | cgo-shim 临时目录与探针中间文件已清理 |
