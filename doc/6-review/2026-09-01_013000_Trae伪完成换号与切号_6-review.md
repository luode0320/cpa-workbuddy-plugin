# 6-review 风格回归：Trae 伪完成检测换号 + 会话分配优先面板选中账号

- 日期：2026-09-01 01:30 (GMT+8)
- 改动范围：`traework/` 11 个文件（10 修改 + 1 新增 `stream_pseudo_test.go`，+214/-8）
  - 功能：`session_auth.go`（+20/-7）、`stream.go`（+45）、`executor.go`（+12）、`accountFailover.go`（+27）、`failover_accounting.go`（+21）、`upstream.go`（+9）
  - 版本/日志：`main.go`、`VERSION`、`CHANGELOG.md`
  - 测试：`session_auth_test.go`（+31）、`upstream_model_test.go`（+26）、`stream_pseudo_test.go`（新增）
- 结论：**STYLE: PASS**

## 检查项

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 格式 / 换行 | PASS | `git diff --check` PASS，无尾随空白 / 冲突标记；新增文件 `stream_pseudo_test.go` 无 gofmt 差异 |
| UTF-8 | PASS | 新增常量注释、日志文案、测试注释均为 UTF-8 中文，无乱码 |
| 命名 | PASS | `isPseudoCompletion` / `noteForcedAccountFailure` / `recordForcedFailure` / `bumpFailoverState` 语义明确，与既有 `noteAccountFailure` / `recordAccountFailure` 命名体系一致；`pseudoCompletionMinChars` 常量跟随既有常量风格 |
| 注释 | PASS | 常量处注释说明"为什么"（伪完成是账号级静默故障、被 resetAccountFailover 清零永不换号）；函数注释说明语义边界（只统计 content 不统计 reasoning）；分支注释说明行为（已生成内容照常下发） |
| 局部写法 | PASS | `if active := getActiveAuthID(); active != ""` 短变量写法贴合既有代码风格；未引入外部模板风格 |
| 测试资产归位 | PASS | 新增 `stream_pseudo_test.go`（`package main` 同包测试）与既有 `*_test.go` 同目录；active 测试追加到既有 `session_auth_test.go`，未新建临时目录 |
| 目录位置 / 依赖方向 | PASS | 改动仅限 `traework/` 包内，无新增依赖 |
| 哨兵 / 临时文件残留 | PASS | 三个行为哨兵（SENTINEL-1/2/3）均已恢复，grep 无残留；临时文件已清理 |
| 改动最小化 | PASS | 仅实现方向 1（active 优先）+ 方向 2（伪完成检测 + 强制记账）+ 版本 bump，未顺手重构 / 改无关代码 |

## 证据

- `cgo-shim build/vet/test` 全绿（`go build` / `go vet` / `go test ./...` 均 OK）
- 行为哨兵 3 连验证（临时移除核心分支 → 对应测试 FAIL，恢复 → 全绿）：
  - SENTINEL-1 禁用 active 优先分支 → `TestPickSessionAuth_FreshAssignmentPrefersActiveID` FAIL（`fresh assignment should prefer panel active account tr-b, got "tr-a"`）
  - SENTINEL-2 禁用伪完成检测 → `TestIsPseudoCompletion` FAIL（`short content below threshold is pseudo: isPseudoCompletion = false, want true`）
  - SENTINEL-3 禁用强制记账 → `TestNoteForcedAccountFailureForPseudo` FAIL（`noteForcedAccountFailure should count a pseudo completion`）
- `git diff --check` PASS；grep `SENTINEL` 无残留

## 说明

- 本 6-review 只判断风格 / 位置 / 格式 / 可读性 / 目录归位，不判断业务正确性、需求覆盖或发布放行。
- 伪完成检测放在 `pumpTraeStream` 的 done 成功分支与 `executor.go` 同步 collect 分支，只拦截「HTTP 200 + done + 极少正文」，不影响 incomplete/断流分支（断流走既有 truncated 处理）。
